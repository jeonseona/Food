[도메인]
askBoard 안에 추가
@ManyToOne
    @JoinColumn(name="no_data", nullable=false)
    private MemberData member;



[persistence]
CustomerServiceRepositorydksdp cnrk
// 마이페이지용(내 질문)
    @Query("SELECT ask FROM askBoard ask JOIN ask.member m WHERE m.id =:id")
    public List<askBoard> myInquiryList(@Param("id") String id);




[service]
CustomerService 
// 마이페이지용(내 질문)
    List<askBoard> getMyInquiry(String id); 

CustomerServiceImpl
// 마이페이지용
    @Override
    public List<askBoard> getMyInquiry(String id) {
    	
    	return customerServiceRepository.myInquiryList(id);
    }

